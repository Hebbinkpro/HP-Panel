<!DOCTYPE html>

<html lang="nl-NL">
    <head>
        <meta charset="utf-8">
        <title>Admin Submissions - HP Panel</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/style.css">
        <link rel="stylesheet" href="/serverPanel/style.css">
    </head>

    <body>
        <div class="header">
            <h1 class="title">HP Panel</h1>
            <h2 class="subtitle">Admin Panel - Submissions</h2>
        </div>

        <div class="profile">
            <h3 class="profile-account"><a href="/account">My Account: <%= account.username %></a></h3>
            <p class="profile-logout">
                <% if(account.admin > 0){ %>
                    <a href="/admin">Admin</a><br>
                <% } %>
                <a href="/console">Dashboard</a><br>
                <a href="/logout">Logout</a>
            </p>
        </div>

        <div class="panelMsg">
            <% if(msg.content !== ""){ %>
                <p style=<%= `color:${msg.color};` %>><%= msg.content %></p>
            <% } %>
        </div>

        <div class="submission">
            <h3>Submission for '<%= submission.name %>'</h3>
            <p><%= new Date(submission.time).toLocaleString("nl-NL", {timeZone: "Europe/Amsterdam", hour12: false}); %></p>
            <p>
                <b>By user:</b>
                <span><a href=<%= `/admin/users/${submission.account.id}` %>><%= submission.account.username %></a></span>
            </p>

            <p>
                <b>Description:</b><br>
                <span><%= submission.desc %></span>
            </p>

            <form method="POST" action=<%= `/admin/submissions/${submission.id}/action` %>>
                <button name="type" value="approve">Approve</button>
                <button name="type" value="reject">Reject</button>
            </form>
        </div>

    </body>
</html>

