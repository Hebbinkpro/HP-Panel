<!DOCTYPE html>

<html lang="nl-NL">
    <head>
        <meta charset="utf-8">
        <title>Account Login - HP Panel</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/style.css">
    </head>

    <body>
        <div class="header">
            <h1 class="title">HP Panel</h1>
            <h2 class="subtitle">Account - Login</h2>
        </div>

        <div class="panelMsg">
            <% if(msg.content !== ""){ %>
                <p style=<%= `color:${msg.color};` %>><%= msg.content %></p>
            <% } %>
        </div>

        <div class="login">
            <form class="form" action="/login" method="POST">
                <label>Username or email:</label><input type="text" name="username" required><br>
                <label>Password:</label><input type="password" name="password" required><br>
                <button type="submit">Login</button>
            </form>

            <p class="login-forgotPassword"><a href="/login">Forgot your password?</a></p>
            <p class="login-registerMsg">You don't have an account? <a href="/login/register">Register</a></p>
        </div>
    </body>
</html>

