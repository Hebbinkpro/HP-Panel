<!DOCTYPE html>

<html lang="nl-NL">
    <head>
        <meta charset="utf-8">
        <title>Submit Project - HP Panel</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/style.css">
    </head>

    <body>
        <div class="header">
            <h1 class="title">HP Panel</h1>
            <h2 class="subtitle">Submit Project</h2>
        </div>

        <div class="profile">
            <h3 class="profile-account"><a href="/account">My Account: <%= account.username %></a></h3>
            <p class="profile-logout">
                <% if(account.admin > 0){ %>
                    <a href="/admin">Admin</a><br>
                <% } %>
                <a href="/console">Dashboard</a><br>
                <a href="/logout">Logout</a>
            </p>
        </div>

        <div class="panelMsg">
            <% if(msg.content !== ""){ %>
                <p style=<%= `color:${msg.color};` %>><%= msg.content %></p>
            <% } %>
        </div>

        <div class="createServer">

            <form class="form" action="/createServer" method="POST" enctype="multipart/form-data">
                <label>Project name:</label><br><input type="text" name="name" required><br><br>
                <label>Description:</label><br><textarea name="desc" required></textarea><br><br>
                <label>Start file: (The file that will be executed when the project starts)</label><br><input type="text" value="index.js" name="startFile" required><br><br>
                <label>Upload the code of your project (as zip)</label><br><input type="file" name="file" accept=".zip"><br><br>
                <button type="submit">Submit</button>

            </form>
        </div>
    </body>
</html>

